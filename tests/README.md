# Test Configuration
To run pytest, a selenium driver is required. Since this driver is dependent on the
web browser (firefox, safari, etc...) used by the developer, it must be set up prior
to running the tests.
